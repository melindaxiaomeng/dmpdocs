# DMP 数据中台 - 最简 MVP 需求文档

> **版本**: MVP v1.0  
> **日期**: 2026-06-23  
> **目标**: 实现最核心的数据接入 → 清洗 → 渠道打包 → 广告计划 → 绑定送量流程

---

## 业务全景图（最简版）

```
┌────────────────────────────────────────────────────────────────┐
│                    DMP 最简 MVP 业务流程                         │
├────────────────────────────────────────────────────────────────┤
│                                                            │
│  【第一步：数据接入】                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │ 程序化广告 │  │ MMP回调   │  │ 文件导入   │               │
│  │ 流量请求   │  │(AppFlyer)│  │(CSV/JSON) │               │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘               │
│       │              │              │                        │
│       └──────────────┴──────────────┘                        │
│                      │                                        │
│                      ▼                                        │
│              【数据清洗 & 去重】                                 │
│              （去重 · 过滤 · 标准化）                           │
│                      │                                        │
├────────────────────────────────────────────────────────────────┤
│                      │                                        │
│  【第二步：渠道打包】                                          │
│                                                            │
│    运营配置渠道规则  ──→  满足条件的用户  ──→  生成渠道          │
│    （地区 / OS / 设备属性 / 自定义标签）                      │
│                      │                                        │
│                      ▼                                        │
│              【渠道管理】                                       │
│              （渠道列表 · 渠道详情 · 渠道状态）                  │
│                      │                                        │
├────────────────────────────────────────────────────────────────┤
│                      │                                        │
│  【第三步：广告计划管理】                                      │
│                                                            │
│    API 对接  ──→  广告计划入库  ──→  广告计划列表            │
│    （Offerwall API / 自定义 API）                             │
│                      │                                        │
│                      ▼                                        │
│              【渠道 × 广告计划绑定】                             │
│                                                            │
│    运营选择渠道 + 广告计划  ──→  开启送量  ──→  数据输出    │
│                                                            │
└────────────────────────────────────────────────────────────────┘
```

---

## 核心功能模块

### 模块一：数据来源管理

#### 1.1 支持的数据来源类型

| 来源类型 | 描述 | 接入方式 |
|----------|------|----------|
| **程序化广告流量请求** | 来自 SSP/Exchange 的 Bid Request | OpenRTB API / Kafka |
| **MMP 回调** | AppFlyer、Adjust 等安装回调 | S2S Postback / Webhook |
| **文件导入** | 上游卖家提供用户数据 | 文件上传（CSV/JSON）/ SFTP 拉取 |

#### 1.2 数据来源配置

**新增来源表单：**

```
来源名称*          [_______________________]
来源类型*          [下拉：流量请求 / MMP回调 / 文件导入]
描述备注           [_______________________]
关联地区           [多选：全球各区域]
```

**接入配置（按类型）：**

- **流量请求**: Endpoint URL、Token 认证、字段映射
- **MMP 回调**: Postback URL 自动生成、事件类型配置
- **文件导入**: 文件格式、字段映射、定时拉取配置

---

### 模块二：数据清洗规则

#### 2.1 为什么需要数据清洗？

**问题场景：**
- 同一设备 ID 格式不一致（`A1B2-C3D4` vs `a1b2c3d4`）→ 去重失败
- 国家编码不统一（`CN` / `cn` / `China` / `中国`）→ 地区统计错误
- 时间格式混乱（秒级 / 毫秒级 / 字符串）→ 时间窗口去重失效
- 空值、异常值 → 影响数据质量

**清洗目标：**
- ✅ 格式统一（设备 ID、国家、时间、IP）
- ✅ 空值处理（填充默认值 / 丢弃）
- ✅ 数据过滤（只保留需要的数据）
- ✅ 去重（避免重复数据）

---

#### 2.2 清洗规则配置界面

**配置入口：** 数据来源管理 → 编辑来源 → 「清洗规则」Tab

**界面布局：**

```
┌─ 清洗规则配置 ──────────────────────────────────────────────────────┐
│                                                              │
│  ┌─ 系统默认规则（自动执行，不可关闭）─────────────────────┐  │
│  │   ✅ 去除首尾空格                                      │  │
│  │   ✅ 设备 ID 标准化（大写 + 去分隔符）                   │  │
│  │   ✅ IP 地址归一化                                     │  │
│  │   ✅ 时间统一为 UTC 毫秒戳                              │  │
│  │   ✅ 国家编码 ISO 3166 标准化                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌─ 可选规则（按需开启）───────────────────────────────┐  │
│  │                                                              │  │
│  │   [+ 添加规则]                                               │  │
│  │                                                              │  │
│  │  ① 缺失值处理                                              │  │
│  │     字段：device_id   策略：[丢弃整条数据 ▼]                     │  │
│  │                                                              │  │
│  │  ② 地区过滤                                                │  │
│  │     仅保留 [US, JP, KR ▼] 地区的数据                         │  │
│  │                                                              │  │
│  │  ③ 时间范围过滤                                            │  │
│  │     仅保留最近 [7] 天的数据                                 │  │
│  │                                                              │  │
│  │  ④ 去重规则                                                │  │
│  │     按 [设备 ID ▼] 去重   时间窗口：[24] 小时               │  │
│  │                                                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                              │
│  [保存规则]  [预览清洗效果]                                     │
└──────────────────────────────────────────────────────────────────┘
```

---

#### 2.3 系统默认清洗规则（自动执行）

**无需配置，所有数据源自动启用：**

| 规则 | 作用 | 处理效果 |
|------|------|----------|
| **去除首尾空格** | 清理字符串前后空格、不可见字符 | `"  abc "` → `"abc"` |
| **设备 ID 标准化** | 转大写、移除横杠/下划线 | `a1b2-c3d4` → `A1B2C3D4` |
| **IP 地址归一化** | 去除端口号、前导零 | `192.168.001.001:8080` → `192.168.1.1` |
| **时间统一 UTC** | 任意格式 → UTC 毫秒戳 | `2024-06-01 12:00:00` → `1717214400000` |
| **国家编码标准化** | 各种格式 → ISO 二位码 | `china` / `中国` → `CN` |

---

#### 2.4 可选清洗规则（按需开启）

##### 2.4.1 缺失值处理

**适用场景：** 核心字段为空时，决定如何处理*

| 策略 | 说明 | 适用字段 |
|------|------|----------|
| **丢弃整条数据** | 字段为空时，整条数据不入库 | device_id、event_time |
| **填充默认值** | 字段为空时，填充指定默认值 | campaign_id、channel_id |
| **标记缺失** | 不填充，仅标记该字段缺失 | 非核心字段 |

**配置示例：**
```
规则类型：缺失值处理
目标字段：device_id
策略：  丢弃整条数据
```

---

##### 2.4.2 数据过滤

**适用场景：** 只保留需要的数据，丢弃不符合条件的数据*

| 过滤类型 | 说明 | 配置示例 |
|----------|------|----------|
| **地区过滤** | 只保留指定国家/地区的数据 | 仅保留 [US, JP, KR] |
| **设备类型过滤** | 只保留指定设备类型 | 仅保留 [Android, iOS] |
| **时间范围过滤** | 只保留指定时间范围的数据 | 仅保留最近 7 天 |
| **自定义条件过滤** | 按任意字段组合条件过滤 | event_type = "install" AND revenue > 0 |

**配置示例：**
```
规则类型：地区过滤
允许的地区：US, JP, KR
动作：  仅保留匹配的数据
```

---

##### 2.4.3 异常值处理

**适用场景：** 数值字段超出合理范围时，决定如何处理*

| 策略 | 说明 | 示例 |
|------|------|----------|
| **截断到边界值** | 超出范围时取边界值 | 101 → 100（最大值 100） |
| **置为空值** | 超出范围时置为 null | 101 → null |
| **丢弃整条数据** | 超出范围时丢弃 | 101 → 丢弃 |

**配置示例：**
```
规则类型：异常值处理
目标字段：revenue（收入）
最小值：0
最大值：99999
超出范围策略：截断到边界值
```

---

##### 2.4.4 去重规则（重要）

**适用场景：** 避免同一设备重复入库*

| 去重模式 | 说明 | 适用场景 |
|----------|------|----------|
| **按设备 ID 去重** | 同一设备 ID 只保留一条 | 通用 |
| **时间窗口去重** | 指定时间内（如 24 小时），同一设备只保留一条 | 避免频繁重复 |
| **周期性去重** | 按天/周/月独立去重，跨周期自动重置 | 每日去重、每周去重 |

**配置示例 1：按设备 ID 去重（全局永久）**
```
规则类型：去重
去重键：  device_id
去重范围：全局永久
保留策略：保留第一条
```

**配置示例 2：24 小时时间窗口去重**
```
规则类型：去重
去重键：  device_id + event_type
时间窗口：24 小时
去重范围：全局
保留策略：保留第一条
```

---

##### 2.4.5 字段映射（枚举字典）

**适用场景：** 将不同数据源的字段值统一为 DMP 标准值*

**配置示例：**
```
规则类型：字段映射
目标字段：event_type
映射关系：
  install   → activation
  purchase  → payment
  sign_up   → register
未匹配时：保留原值
```

---

#### 2.5 清洗效果预览

**配置完成后，点击「预览清洗效果」：**

| 指标 | 说明 |
|------|------|
| **清洗成功率** | 成功入库条数 / 总输入条数 |
| **丢弃率** | 被丢弃条数 / 总输入条数 |
| **去重剔除率** | 去重丢弃条数 / 总输入条数 |
| **字段缺失率** | 各字段空值比例 |

**预览界面：**

```
┌─ 清洗效果预览 ──────────────────────────────────────────────────────┐
│  总输入：10,000 条                                           │
│  成功入库：9,500 条  （成功率 95%）                         │
│  丢弃：500 条  （丢弃率 5%）                               │
│                                                              │
│  丢弃原因分布：                                             │
│    ├── 设备 ID 为空：300 条                                 │
│    ├── 地区不匹配：150 条                                   │
│    └── 去重剔除：50 条                                    │
│                                                              │
│  [查看原始数据样本]  [查看清洗后数据样本]                     │
└──────────────────────────────────────────────────────────────────┘
```

---

#### 2.6 清洗规则模板（快速配置）

**系统预设模板，一键应用：**

| 模板名称 | 适用场景 | 包含的规则 |
|----------|----------|----------|
| **MMP 回调标准模板** | AppFlyer / Adjust 回调 | 默认规则 + 必填字段校验 + 地区过滤 + 24h 去重 |
| **RTB 流量请求模板** | OpenRTB Bid Request | 默认规则 + 字段类型转换 + 数值精度处理 + 批量去重 |
| **文件导入模板** | CSV / JSON 文件导入 | 默认规则 + 全量字段校验 + 编码转换 + 批次内去重 |

**使用方式：**
1. 新增数据来源时，选择模板
2. 系统自动加载预设规则
3. 可根据需要微调

---

---

### 模块三：渠道打包（核心）

#### 3.1 渠道概念

**渠道** = 满足条件的用户集合（类似"人群包"的简化版）

**用途**: 运营将渠道与广告计划绑定，进行精准送量。

#### 3.2 渠道列表页

| 字段 | 说明 |
|------|------|
| 渠道名称 | 自定义命名 |
| 用户量（估算） | 满足条件的去重设备 ID 数量 |
| 地区分布 | 覆盖的主要国家/地区 |
| 创建时间 | - |
| 状态 | 草稿 / 已就绪 / 送量中 / 已停用 |
| 绑定广告计划数 | 当前绑定的广告计划数量 |
| 操作 | 编辑 / 查看 / 绑定广告计划 / 删除 |

#### 3.3 创建渠道（规则配置）

```
渠道名称*         [______________________]
描述备注          [______________________]

── 打包规则 ──────────────────────────────────────────
  [AND ▼] 条件组

  ├── 地理位置 · 国家  [包含 ▼]  [多选国家 ▼]
  ├── 操作系统          [等于 ▼]  [Android ▼]  [iOS ▼]
  ├── 设备类型          [包含 ▼]  [Phone / Tablet]
  ├── 最近活跃时间      [最近 ▼]  [7] 天内
  └── [+ 添加条件]

  [+ 添加条件组（OR）]

── 右侧预览区 ──────────────────────────────────────
  估算结果：约 [____] 个设备 ID     [立即估算]
  地区分布：[饼图]
  OS 分布：iOS __% / Android __%
────────────────────────────────────────────────────

[保存草稿]  [生成渠道]
```

#### 3.4 渠道详情页

- **基础信息**: 用户总量、地区分布、OS 分布
- **绑定记录**: 已绑定的广告计划列表及送量数据
- **历史快照**: 每次更新的用户量变化

---

### 模块四：广告计划管理

#### 4.1 广告计划来源

**两种方式：**

1. **API 自动对接** - 定时拉取广告计划（Offerwall API）
2. **手动录入** - 运营手动添加广告计划

#### 4.2 支持的 API 对接类型

| API 类型 | 说明 | 示例 |
|----------|------|------|
| **Offerwall API** | 对接广告平台，自动拉取 Offer | Tapjoy、AdColony |
| **自定义 API** | 用户自定义 API 接口 | 自有广告系统 |

#### 4.3 广告计划列表页

| 字段 | 说明 |
|------|------|
| 广告计划 ID | 唯一标识 |
| 广告计划名称 | - |
| 来源 API | 自动拉取 / 手动录入 |
| CPI 出价 ($) | 每次安装结算单价 |
| 目标市场 | 国家/地区 |
| 目标平台 | iOS / Android |
| 状态 | 运行中 / 暂停 / 已结束 |
| 绑定渠道数 | 当前绑定的渠道数量 |
| 操作 | 编辑 / 查看 / 绑定渠道 / 删除 |

#### 4.4 API 对接配置

```
API 名称*          [_______________________]
API 类型*          [Offerwall API ▼ / 自定义 API]
API 地址*          [_______________________]
认证方式           [API Key ▼ / OAuth / 无]
API Key           [_______________________]
拉取频率           [每小时 ▼ / 每天 / 自定义]
字段映射配置        [+ 添加映射]
```

---

### 模块五：渠道 × 广告计划绑定（送量核心）

#### 5.1 绑定流程

```
运营操作台
    │
    ▼
选择渠道  ×  选择广告计划
    │
    ▼
配置送量策略
    │
    ├── 送量比例（该渠道流量分配给此广告计划的比例）
    ├── 每日上限（最大送量数）
    ├── 优先级（多广告计划绑定同一渠道时）
    └── 生效时间
    │
    ▼
开启送量
    │
    ▼
数据输出（API 推送 / 文件导出）
```

#### 5.2 绑定列表页

| 字段 | 说明 |
|------|------|
| 渠道名称 | - |
| 广告计划名称 | - |
| 送量比例 | 百分比 |
| 每日上限 | 最大送量数 |
| 今日已送量 | 当日已输送的量 |
| 优先级 | 数字（越大越优先） |
| 状态 | 送量中 / 暂停 |
| 操作 | 编辑 / 暂停/启动 / 解绑 |

#### 5.3 送量数据输出

**输出方式：**

| 方式 | 说明 |
|------|------|
| **API 推送** | 实时推送设备 ID 到广告计划 API |
| **文件导出** | 定时生成 CSV/JSON 文件 |
| **Postback 接收** | 接收广告计划侧的安装回调 |

---

## 数据流转图

```
┌────────────────────────────────────────────────────────────────┐
│                        数据流转说明                              │
├────────────────────────────────────────────────────────────────┤
│                                                            │
│  1. 数据接入层                                              │
│     数据源 API ──→ 数据接收服务 ──→ 原始数据表              │
│                                                            │
│  2. 数据清洗层                                              │
│     原始数据 ──→ 清洗规则引擎 ──→ 有效数据表                │
│     （去重 · 过滤 · 标准化）                                 │
│                                                            │
│  3. 渠道打包层                                              │
│     有效数据 ──→ 渠道规则匹配 ──→ 渠道用户表                │
│                                                            │
│  4. 广告计划层                                              │
│     外部 API ──→ 广告计划入库 ──→ 广告计划表               │
│                                                            │
│  5. 送量层                                                  │
│     渠道用户 ──→ 绑定关系 ──→ 送量输出 ──→ 安装回调       │
│                                                            │
└────────────────────────────────────────────────────────────────┘
```

---

## 核心页面清单

| 页面 | 路径 | 说明 |
|------|------|------|
| 首页概览 | `/home` | 核心指标卡片 |
| 数据来源列表 | `/data/source` | 来源管理 |
| 新增数据来源 | `/data/source/create` | 抽屉式表单 |
| 数据清洗配置 | `/data/clean` | 清洗规则 |
| 渠道列表 | `/channel/list` | 渠道管理 |
| 创建渠道 | `/channel/create` | 规则配置 |
| 广告计划列表 | `/campaign/list` | 广告计划管理 |
| API 对接配置 | `/campaign/api` | API 配置 |
| 绑定管理 | `/binding/list` | 渠道×广告计划绑定 |
| 送量监控 | `/delivery/monitor` | 送量实时监控 |

---

## MVP 版本不包含的功能（后续迭代）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| **人群包版本管理** | 渠道快照版本对比 | 低 |
| **Lookalike 扩展** | 相似用户扩展 | 低 |
| **DDJ 归因劫持** | S2S 发送抢占归因 | 中 |
| **结算模块** | 收入报表、对账单 | 高（第二阶段） |
| **全球覆盖分析** | 热力地图、覆盖率分析 | 中 |
| **异常对账池** | 数据差异自动排查 | 中 |
| **Margin 风控** | 毛利率熔断机制 | 中 |

---

## 技术选型建议（MVP）

| 用途 | 推荐技术 | 说明 |
|------|----------|------|
| 前端 | React + TypeScript + Ant Design | 快速开发 |
| 后端 API | Node.js (Express) / Go | 高并发接入 |
| 数据库 | MySQL / PostgreSQL | 业务数据 |
| 缓存 | Redis | 设备 ID 去重、渠道用户缓存 |
| 消息队列 | Redis Streams / RabbitMQ | 异步数据处理 |
| 部署 | Docker + Nginx | 简单部署 |

---

## 数据库核心表设计（MVP）

### 1. 数据来源表 (`data_source`)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| name | varchar | 来源名称 |
| type | varchar | 来源类型 |
| config | json | 接入配置 |
| status | tinyint | 状态 |
| created_at | datetime | 创建时间 |

### 2. 清洗规则表 (`clean_rule`)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| source_id | bigint | 关联来源 |
| rule_type | varchar | 规则类型 |
| rule_config | json | 规则配置 |
| created_at | datetime | 创建时间 |

### 3. 渠道表 (`channel`)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| name | varchar | 渠道名称 |
| rules | json | 打包规则 |
| user_count | bigint | 用户量 |
| status | tinyint | 状态 |
| created_at | datetime | 创建时间 |

### 4. 广告计划表 (`campaign`)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| name | varchar | 广告计划名称 |
| api_source | varchar | API 来源 |
| cpi | decimal | CPI 出价 |
| target_countries | json | 目标市场 |
| status | tinyint | 状态 |
| created_at | datetime | 创建时间 |

### 5. 绑定关系表 (`channel_campaign`)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| channel_id | bigint | 渠道 ID |
| campaign_id | bigint | 广告计划 ID |
| delivery_percent | int | 送量比例 |
| daily_limit | int | 每日上限 |
| priority | int | 优先级 |
| status | tinyint | 状态 |
| created_at | datetime | 创建时间 |

### 6. 送量日志表 (`delivery_log`)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| channel_id | bigint | 渠道 ID |
| campaign_id | bigint | 广告计划 ID |
| device_id | varchar | 设备 ID |
| delivered_at | datetime | 送量时间 |
| callback_status | varchar | 回调状态 |

---

## 开发优先级（MVP 阶段）

### 第一优先级（核心流程）

1. ✅ 数据来源管理（增删改查）
2. ✅ 数据清洗规则配置
3. ✅ 渠道打包（规则配置 + 用户估算）
4. ✅ 广告计划管理（手动录入 + API 对接）
5. ✅ 渠道 × 广告计划绑定
6. ✅ 送量数据输出

### 第二优先级（监控与优化）

1. 送量实时监控看板
2. 数据质量报告
3. 清洗效果分析
4. 基础权限管理

---

## 附录：API 接口规范（MVP）

### 1. 数据接入 API

#### 1.1 Postback 接收接口

```
POST /api/v1/source/{source_id}/postback
```

**请求参数：**

```json
{
  "event": "install",
  "device_id": "aabbccdd-1234-5678-abcd-123456789012",
  "device_id_type": "gaid",
  "ip": "1.2.3.4",
  "country": "US",
  "app_id": "com.example.game",
  "event_time": 1719123456
}
```

**响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "device_id": "aabbccdd-****-****-****-123456789012",
    "clean_status": "passed",
    "channel_matched": ["channel_001", "channel_003"]
  }
}
```

#### 1.2 广告计划 API（对外）

```
GET /api/v1/campaign/{campaign_id}/devices
```

**响应示例：**

```json
{
  "code": 0,
  "data": {
    "campaign_id": "cmp_001",
    "devices": [
      {
        "device_id": "aabbccdd-****-****-****-123456789012",
        "device_id_type": "gaid",
        "country": "US",
        "os": "android"
      }
    ],
    "has_more": true,
    "next_cursor": "xyz123"
  }
}
```

---

## 总结

**MVP 版本核心价值：**

1. **简化流程** - 去除复杂特性，聚焦核心链路
2. **快速上线** - 预计 4-6 周完成 MVP 开发
3. **可扩展** - 预留接口，后续可无缝升级到完整版

**核心差异（vs 完整版）：**

| 维度 | MVP 版 | 完整版 |
|------|--------|--------|
| 渠道打包 | 基础规则 | 复杂条件 + 版本管理 |
| 广告计划 | 基础管理 | DSP 精准投放 + DDJ 归因劫持 |
| 结算模块 | 无 | 完整对账系统 |
| 数据分析 | 基础监控 | 全球覆盖分析 + 异常对账 |
| 风控系统 | 无 | Margin 熔断 + AI 防欺诈 |

---

> **文档信息**
>
> | 项目 | 内容 |
> |------|------|
> | 文档名称 | DMP 数据中台 - 最简 MVP 需求文档 |
> | 版本 | MVP v1.0 |
> | 日期 | 2026-06-23 |
> | 核心变化 | 简化流程，聚焦数据源→清洗→渠道打包→广告计划→绑定送量 |
